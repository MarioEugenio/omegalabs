@model IEnumerable<OmegaLabsZero.Models.SUB_SUBSTANCIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SUB_NOME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DER_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SUB_DETALHES)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STA_STATUS.STA_NOME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SUB_NOME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DER_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUB_DETALHES)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STA_STATUS.STA_NOME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SUB_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SUB_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SUB_ID })
        </td>
    </tr>
}

</table>
